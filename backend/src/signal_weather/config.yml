service:
  name: polystrategy-signal-weather
  port: 8001

consul:
  tags:
    - "traefik.enable=true"
    - "traefik.http.routers.polystrategy-signal-weather.rule=PathPrefix(`/api/weather`)"
    - "traefik.http.routers.polystrategy-signal-weather.entrypoints=web"
    - "traefik.http.routers.polystrategy-signal-weather.middlewares=forward-auth@file"
  health_path: /health

dependencies: []
