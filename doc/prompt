一。对目前目前想实施的三个策略的说明

三个策略，以单子的记录一个策略对一tokenid的行为操作

策略1：扫单信号下单
1.收到扫单信号后，挂buy单0.99价格的固定份额（用固定好ticksize等一系列参数的快速下单程序），同时开启风控模块
2.挂单一定时间之后（可以自己定义），撤回挂单
3.撤回挂单之后，由于风控模块会监听订单簿，监听时发现ticksize变成0.001时，挂0.999价格的sell单，用已经有的全部份额（注意可能订单簿监听返回ticksize=0.001，但是实际没有。监听订单簿用的是websockt推送，下sell单之前，先通过http调用主动获取ticksize检查一下，并设立好失败重试机制）
4.所有的0.999的share卖出，或者触发风控时，结束这个单子。

策略2：leader信号下单
1.收到leader下单后吗，挂buy单0.99价格的（用固定好ticksize等一系列参数的快速下单程序），同时开启风控模块（份额可以定义，两种定义，一种固定份额，一种用余额计算出的最大份额）
2.挂单一定时间之后（可以自己定义），撤回挂单（如果没有任何成交，此时可以结束这个单子）
3.撤回挂单之后，由于风控模块会监听订单簿，监听时发现ticksize变成0.001时，挂0.999价格的sell单，用已经有的全部份额（注意可能订单簿监听返回ticksize=0.001，但是实际没有。监听订单簿用的是websockt推送，下sell单之前，先通过http调用主动获取ticksize检查一下，并设立好失败重试机制）
4.所有的0.999的share卖出，或者触发风控时，结束这个单子。


策略3：策略1，2结合
1.收到扫单信号后，挂buy单0.99价格的固定份额（用固定好ticksize等一系列参数的快速下单程序），同时开启风控模块
2.挂单一定时间之后（可以自己定义），若没有收到leader信号，进行退场，退场逻辑后续详细描述；若收到，追加下单，追加用用余额计算出的最大份额继续下单，然后等待一定时间。
3.退场逻辑有两种，首先经历步骤2之后，没有任何成交的份额，结束该单。如果有继续单子，有退场逻辑1或者2，可以选择
5.退场逻辑1：后续使用上述的0.999卖出+风控的处理策略
6退场逻辑2：挂单0.99价格卖出，如果全部卖出，则结束该单，没有全部卖出用风控兜底

二。改造的一些要求

一些要求：1.记录好一个单子的全过程，前端要可以展示处理
2.一个策略可以自定义的参数，要前端可以配置，比如风控比例，等待时间，退场策略等等
3.固定份额时，可能此时余额不够，用余额-5的支持的最大份额下单，注意余额为真实的余额 减去 之前账户buy单还没成交的部分，为了快速下单余额要提前维护在内存里，采用定时轮询的方式维护（如果轮询间发生下单，可以将余额直接减去）
4.对于策略2，多一个配置，去过滤leader的yestoken或者notoken的activity信号，或者不过滤，predexon的推送中应该有著名其是yes还是no的buy
5.对于收到leader信号和sweep信号时，使用快速下单方法，其他的使用正常的下单模式


风控模块：
1。记录买入这个token的平均价格（开启风控之后可能还会有成交，这个平均价格是变化的）
2。开启时持续监听订单簿
3。若果没有买入放入平均价格，用挂buy单的价格（虽然大概不会发生这种情况）
4。如果这个订单簿的best bid价格为平均买入价格的60%，强制平仓（这个可以自己定义）
5。关闭风控模块，需要没有buy单已经没有持仓时，才可以可以关闭（可以预留强制关闭接口）

三。当前的计划

整理出一个需求说明文档，和实际的修改（计划实施）文档

根据上述说明制定一个修改计划包括代码，数据等等的要求：
1.用流程图描述出来策略1，2，3
2.计划中包含数据库对应的设计，前端对应的设计，
3.后续可能有别的策略，需要设计的架构方便拓展
4计划应该是逐策略，逐步进行的，方便git一步步的提交
5.前端可以选择用户和策略
6.我希望采用不同的微服务模块，比如天气订单簿监听和leader监听实际上是给策略下单模块提供信号源，不同的策略也可以是一个微服务，分析都需要设计哪些微服务
7.由于下单有很强的速度要求，因此要优先考虑微服务模块间的通信速度，用内存还是网络（我的期望延迟在10ms之内），数据结构是什么样的？
8.注意策略1，2，3之间存在较多的逻辑上的重合，但是分类上是三个策略，如何高内聚低耦合，是很大的设计难题
9.需要说明出每个策略，目前可以调整的参数有哪些。
10.目前主要推进策略1，但是要先设计好一个统一的架构，要求信号获取和策略都用一个微服务进行，对于这样项目而言，目前主要是两个信号，三个策略，共5个微服务
11.需求文档，和计划实施，写到doc中，并用日期+文件名的形式命名