# 策略执行系统重构计划 — 从框架到容器+工具箱

## 目标

将 `strategy_execution` 从「规定策略怎么跑的框架」重构为「提供能力的容器+工具箱」，使得：

1. **任何市场**（天气/体育/电竞/政治）的策略都能接入
2. **任何逻辑**（扫单/跟单/做市/套利/事件驱动）的策略都能实现
3. **每个策略和信号源**都是独立微服务，互不影响
4. 新增策略不需要改动 runtime 或基础设施代码

---

## 现有问题

| 问题 | 具体表现 |
|------|---------|
| BaseStrategy 绑定特定事件类型 | `on_leader_signal`, `on_tick_candidate` — SweepStrategy 被迫实现空方法 |
| 状态机固定 | 6 个状态 + 固定转换表，做市策略不适用 |
| 风控强制注入 | 所有策略必须用 `RiskManager`(止损逻辑)，但套利策略的风控是对冲敞口 |
| 信号源硬编码 | runtime 里写死 `NEEDS_WEATHER` / `NEEDS_LEADER` |
| 资金管理强制 | 必须用 `AccountExecutionLedger`，但有些策略可能跨账户操作 |

---

## 新架构设计

### 总体结构

```
backend/src/
├── strategy_runtime/              ← 极简容器（新建，替代 strategy_execution）
│   ├── __init__.py
│   ├── interfaces.py              ← 核心协议定义
│   ├── container.py               ← 策略进程容器（启动/停止/健康检查）
│   ├── signal_bus.py              ← 通用信号接收与路由
│   ├── state_store.py             ← 运行状态持久化（策略自定义状态）
│   └── app_factory.py             ← FastAPI app 工厂
│
├── toolkit/                       ← 可选工具箱（策略按需引入）
│   ├── __init__.py
│   ├── execution/
│   │   ├── order_executor.py      ← Polymarket CLOB 下单（从现有迁移）
│   │   ├── order_tracker.py       ← 订单生命周期跟踪
│   │   └── account_ledger.py      ← 资金/份额管理（从现有迁移）
│   ├── risk/
│   │   ├── stop_loss.py           ← 止损风控（从现有 RiskManager 提取）
│   │   ├── trailing_stop.py       ← 追踪止损（未来）
│   │   └── exposure_limit.py      ← 敞口限制（未来）
│   ├── market/
│   │   ├── tick_verifier.py       ← tick=0.001 校验（从现有迁移）
│   │   └── bbo_monitor.py         ← BBO 实时监控
│   └── signals/
│       ├── ws_client.py           ← 通用 WS 信号客户端
│       └── dedup.py               ← TTL 去重器（从现有迁移）
│
├── strategy_sweep/                ← 天气扫单策略
│   ├── app.py
│   ├── strategy.py
│   └── config.py                  ← 策略自有参数定义
├── strategy_leader/               ← 跟单策略
│   ├── app.py
│   ├── strategy.py
│   └── config.py
├── strategy_sweep_leader/         ← 组合策略
│   ├── app.py
│   ├── strategy.py
│   └── config.py
│
├── signal_weather_orderbook/      ← 天气信号源微服务（已有）
├── signal_leader_activity/        ← Leader 信号源微服务（已有）
└── signal_xxx/                    ← 未来新信号源...
```

### 核心接口定义 (`strategy_runtime/interfaces.py`)

```python
from __future__ import annotations
from abc import ABC, abstractmethod
from dataclasses import dataclass, field
from typing import Any, ClassVar, Protocol

# ─── 通用信号 ─────────────────────────────────────────────────

@dataclass(frozen=True)
class Signal:
    """所有信号源必须输出此格式。"""
    signal_id: str              # 全局唯一 ID
    signal_type: str            # "sweep", "leader_buy", "news", "arb_opportunity"
    token_id: str               # Polymarket token
    market_slug: str            # 所属市场
    occurred_at_ms: int         # 信号产生时间
    source: str                 # 信号源标识: "weather_orderbook", "sports_monitor"
    payload: dict = field(default_factory=dict)  # 信号源特有数据


# ─── 策略协议 ─────────────────────────────────────────────────

class BaseStrategy(ABC):
    """策略最小约束 — 只规定生命周期，不规定内部实现。"""

    # 策略声明订阅哪些信号类型（runtime 据此决定连接哪些信号源）
    SUBSCRIBED_SIGNALS: ClassVar[set[str]] = set()

    @abstractmethod
    async def start(self, ctx: "StrategyContext") -> None:
        """初始化：策略从 ctx 获取需要的工具，设置内部状态。"""

    @abstractmethod
    async def on_signal(self, signal: Signal) -> None:
        """收到信号 — 策略自行决定如何处理。"""

    @abstractmethod
    async def stop(self) -> None:
        """关闭：取消定时器、撤销挂单、释放资源。"""


# ─── 策略上下文 ────────────────────────────────────────────────

@dataclass
class StrategyContext:
    """工具箱入口 — 策略按需使用，不强制全部依赖。"""
    executor: "OrderExecutorProtocol"      # 下单/撤单
    state_store: "StateStoreProtocol"      # 自定义状态持久化
    config: dict[str, Any]                 # 策略配置参数
    proxy_wallet: str                      # 执行钱包
    run_id: str                            # 本次运行 ID


# ─── 能力协议（工具箱内各组件的接口） ──────────────────────────

class OrderExecutorProtocol(Protocol):
    async def place_order(self, token_id: str, side: str, price: str,
                          size: str, **kwargs) -> "OrderResult": ...
    async def cancel_order(self, order_id: str) -> bool: ...

class StateStoreProtocol(Protocol):
    async def save(self, run_id: str, state: dict) -> None: ...
    async def load(self, run_id: str) -> dict | None: ...

@dataclass(frozen=True)
class OrderResult:
    order_id: str
    status: str                # "filled", "partial", "live", "failed"
    filled_size: str
    filled_price: str | None
```

### 信号总线 (`strategy_runtime/signal_bus.py`)

```python
class SignalBus:
    """通用信号接收 — 连接一个或多个信号源 WS，转为 Signal 后分发。"""

    def __init__(self, sources: list[SignalSourceConfig]):
        self._sources = sources  # [{url, signal_type, adapter_class}]

    async def start(self, handler: Callable[[Signal], Awaitable[None]]) -> None:
        """为每个信号源启动 WS 连接，消息经 adapter 转为 Signal 后回调 handler。"""

    async def stop(self) -> None: ...
```

### 策略容器 (`strategy_runtime/container.py`)

```python
class StrategyContainer:
    """极简容器 — 只管生命周期和信号路由。"""

    def __init__(
        self,
        strategy_class: type[BaseStrategy],
        config: dict,
        signal_sources: list[SignalSourceConfig],
    ):
        ...

    async def start(self) -> None:
        """
        1. 创建 StrategyContext（注入工具箱组件）
        2. 启动 SignalBus（只连接策略声明的信号类型）
        3. 调用 strategy.start(ctx)
        """

    async def stop(self) -> None:
        """strategy.stop() → SignalBus.stop()"""
```

---

## 信号源适配器

每个信号源微服务产出的消息格式不同，需要一个轻量适配器转为通用 `Signal`：

```python
# toolkit/signals/adapters/weather_adapter.py
class WeatherSweepAdapter:
    """将天气信号服务的 weather_sweep 消息转为通用 Signal。"""

    def adapt(self, raw: dict) -> Signal:
        data = raw.get("signal", {})
        return Signal(
            signal_id=data["event_id"],
            signal_type="sweep",
            token_id=data["token_id"],
            market_slug=data.get("market_slug", ""),
            occurred_at_ms=data["occurred_at_ms"],
            source="weather_orderbook",
            payload=data,
        )


# toolkit/signals/adapters/leader_adapter.py
class LeaderBuyAdapter:
    def adapt(self, raw: dict) -> Signal:
        data = raw.get("signal", {})
        return Signal(
            signal_id=data["event_id"],
            signal_type="leader_buy",
            token_id=data["token_id"],
            market_slug=data.get("market_slug", ""),
            occurred_at_ms=data["occurred_at_ms"],
            source="leader_activity",
            payload=data,
        )
```

---

## 现有策略改写示例

### SweepStrategy（改写后）

```python
from strategy_runtime.interfaces import BaseStrategy, Signal, StrategyContext
from toolkit.risk.stop_loss import StopLossMonitor
from toolkit.market.tick_verifier import TickVerifier

class SweepStrategy(BaseStrategy):
    SUBSCRIBED_SIGNALS = {"sweep"}

    async def start(self, ctx: StrategyContext) -> None:
        self.ctx = ctx
        self.state = "idle"
        self.position_shares = Decimal("0")
        self.entry_order_id = None

        # 按需创建工具
        self.risk = StopLossMonitor(
            ratio=Decimal(ctx.config.get("stop_loss_ratio", "0.6")),
            on_trigger=self._risk_exit,
        )
        self.tick_verifier = TickVerifier()

    async def on_signal(self, signal: Signal) -> None:
        if signal.signal_type == "sweep" and self.state == "idle":
            await self._enter(signal)
        elif signal.signal_type == "bbo_update":
            await self.risk.check(signal.payload)
        elif signal.signal_type == "tick_change":
            await self._check_tick_exit(signal)

    async def stop(self) -> None:
        if self.entry_order_id:
            await self.ctx.executor.cancel_order(self.entry_order_id)

    async def _enter(self, signal: Signal) -> None:
        fixed_shares = self.ctx.config.get("fixed_entry_shares", "100")
        result = await self.ctx.executor.place_order(
            token_id=signal.token_id,
            side="BUY",
            price="0.99",
            size=fixed_shares,
        )
        if result.status == "filled":
            self.state = "holding"
            self.position_shares = Decimal(result.filled_size)
            self.risk.start(entry_price=Decimal(result.filled_price))
        ...

    async def _risk_exit(self) -> None:
        await self.ctx.executor.place_order(
            token_id=..., side="SELL", price="0.01", size=str(self.position_shares))
        self.state = "closed"

    async def _check_tick_exit(self, signal: Signal) -> None:
        if await self.tick_verifier.verify(signal.payload["token_id"]):
            await self.ctx.executor.place_order(
                token_id=..., side="SELL", price="0.999", size=str(self.position_shares))
            self.state = "closed"
```

---

## 实施阶段

### Phase 1: 定义核心接口（1天）
- 创建 `strategy_runtime/interfaces.py` — Signal, BaseStrategy, StrategyContext, Protocol
- 创建 `strategy_runtime/container.py` — 极简容器
- 创建 `strategy_runtime/signal_bus.py` — 通用信号总线
- 创建 `strategy_runtime/state_store.py` — 状态持久化
- 创建 `strategy_runtime/app_factory.py` — FastAPI app 生成

### Phase 2: 提取工具箱（1天）
- `toolkit/execution/order_executor.py` ← 从现有 `execution/order_executor.py` 迁移，接口简化
- `toolkit/execution/account_ledger.py` ← 从现有迁移
- `toolkit/risk/stop_loss.py` ← 从现有 `risk/manager.py` 提取止损逻辑
- `toolkit/market/tick_verifier.py` ← 从现有迁移
- `toolkit/signals/ws_client.py` ← 通用 WS 客户端
- `toolkit/signals/dedup.py` ← 从现有 `signal_subscription.py` 提取
- `toolkit/signals/adapters/` ← 天气/Leader 适配器

### Phase 3: 重写 3 个策略（2天）
- `strategy_sweep/strategy.py` — 用新接口重写，内部用 toolkit 组件
- `strategy_leader/strategy.py` — 同上
- `strategy_sweep_leader/strategy.py` — 同上
- 每个策略的 `app.py` 改为使用 `StrategyContainer`

### Phase 4: 清理旧代码（0.5天）
- 删除 `strategy_execution/` 整个目录
- 更新信号源（天气/Leader）的输出格式或添加适配器
- 验证所有微服务独立启动正常

### Phase 5: 验证（1天）
- 每个策略独立启动测试
- 信号端到端测试（信号源 → 信号总线 → 策略 → 下单）
- 停一个策略不影响其他策略
- 模拟新市场信号源接入（确认不需要改 runtime）

---

## 关键设计决策

| 决策 | 选择 | 原因 |
|------|------|------|
| 策略状态持久化 | 策略自定义 state dict，container 帮忙存 | 不同策略状态结构完全不同 |
| 风控 | 工具箱可选组件，策略自己决定用不用 | 做市策略和趋势策略风控逻辑完全不同 |
| 信号过滤 | 策略 on_signal 内部自己过滤 | runtime 不该知道业务过滤规则 |
| 订单管理 | 工具箱提供 executor，策略自己管理订单生命周期 | 有些策略同时挂多单，有些只有一单 |
| 多运行防重 | 策略自己实现（或用工具箱的 guard 组件） | 不是所有策略都需要防重 |
| 数据库表 | 通用 strategy_runs 表 + 策略私有表 | 运行记录格式统一，策略特有数据各管各 |

---

## 新增策略的步骤（重构后）

```bash
# 1. 创建策略包
mkdir backend/src/strategy_xxx/

# 2. 写 3 个文件
strategy_xxx/
├── __init__.py
├── app.py          # 10行: 声明信号源 + 启动容器
├── strategy.py     # 实现 start/on_signal/stop
└── config.py       # 策略参数（可选）

# 3. 启动
python src/strategy_xxx/app.py

# 不需要改 runtime、不需要改其他策略、不需要改信号源
```
